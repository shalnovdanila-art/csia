
  # Nutrition Planning App
  
    Open the hosted version here:
    https://csia.pages.dev/

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  